import maths 

-----------------------------------------------------------------------------

1--)math.fabs()

x = -25
print(math.fabs(x)) ==> 25
        OR
print(x.abs) ==> 25.0  ( difference is this time we didn't use module )

-----------------------------------------------------------------------------

2--)math.factorial()

print(math.factorial(5)) ==> 120

-----------------------------------------------------------------------------

3--)math.pow()

math.pow(2,5) ==> 32
math.pow(10,2) ==> 100 

*********************************************************************************
*********************************************************************************

import random

-----------------------------------------------------------------------------

1-)random.random()   
   
   x = random.random()
   print(x) ===> this show number which between 0-1
   
-----------------------------------------------------------------------------

2-)random.randit()
  
  this time this function show number which integer not float but we have to define range
  random.randit(1,10)

-----------------------------------------------------------------------------

3-)random.choice()

list = ["phone","computer","tablet"]
x = random.choice(list) show that random one item of list.

*********************************************************************************
*********************************************************************************




